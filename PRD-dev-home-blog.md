# 产品需求说明书 · 复古终端风个人博客

> **项目代号**：`dev@home`
> **文档版本**：v1.0
> **最后更新**：2026-07-10
> **作者**：dev@home（Go 后端工程师）
> **文档状态**：原型确认阶段（Prototype Approved）

---

## 1. 项目概述

### 1.1 背景
个人需要一个「数字自留地」——集**个人介绍、职业经历、思考随笔、项目展示、全球足迹、技术笔记/博客**于一体的个人主页。要求现在能**免费托管**上线，未来能**平滑迁移**到自有服务器与自有域名，不被平台锁定。

### 1.2 目标
- 一个视觉独特、有记忆点的个人品牌主页，区别于千篇一律的模板站。
- 采用**复古终端 / CRT 绿磷荧光**主题，贴合后端工程师气质。
- 全站**单文件、零构建、离线可跑**（原型阶段），核心交互不依赖任何外部运行时。
- 移动端与桌面端均可正常浏览与交互。

### 1.3 非目标（本期不做）
- 后台 CMS / 登录鉴权 / 评论系统。
- 服务端渲染与动态数据库。
- 多语言 i18n（当前中英混排即可）。

---

## 2. 目标用户与场景

| 用户 | 场景 | 诉求 |
|---|---|---|
| 招聘方 / 同行 | 通过链接了解本人 | 快速看到经历、项目、技术栈 |
| 朋友 / 访客 | 随手逛逛 | 有趣的交互（转地球看足迹） |
| 本人 | 长期维护 | 用 Markdown 写博客，改内容成本低 |

---

## 3. 设计规范（Design System）

### 3.1 主题
**Retro Terminal / CRT（复古终端）** —— 绿磷荧光显示器美学。

### 3.2 视觉规范
| 项 | 值 |
|---|---|
| 背景 | `#0a0e0a`（近黑绿） |
| 面板底 | `#0d140d` |
| 主色（绿磷） | `#4ade80` |
| 强调色（琥珀） | `#fbbf24` |
| 正文 | `#c8f5d3` |
| 次要文字 | `#5a7d63` |
| 标题字体 | Space Grotesk |
| 等宽/终端字体 | IBM Plex Mono |

### 3.3 特效
- **CRT 扫描线**：全屏叠加的横向扫描线纹理 + 边缘暗角（vignette）。
- **屏幕闪烁**：极轻微的周期性亮度抖动（`flick` 动画，7s 周期）。
- **命令行语义**：区块标题用 `$ command` 前缀（如 `$ cat about.md`），路径面包屑 `~/globe/CN/湖南省` 风格。
- **发光**：主色元素带 `drop-shadow` / `box-shadow` 荧光辉光。

### 3.4 响应式
- 断点：`max-width: 600px` 走移动端规则。
- 字号用 `clamp()` 自适应；地球 canvas 尺寸 `min(innerWidth-40, 440)`。
- 触摸事件完整支持（拖拽旋转、点击下钻）。

---

## 4. 信息架构（页面模块）

单页滚动 + 顶部锚点导航，模块顺序如下：

| # | 模块 | 命令语义 | 内容 |
|---|---|---|---|
| 0 | 顶部导航 | `dev@home:~` | 吸顶 + 毛玻璃，锚点跳转各区 |
| 1 | Hero 首屏 | `$ ssh dev@home --connect` | 姓名、职位、一句话简介、闪烁光标、快捷入口 |
| 2 | whoami | `$ cat about.md` | 自我介绍、技术栈 |
| 3 | 经历 | `$ tail -f experience.log` | 终端日志风时间线 |
| 4 | 思考 | `$ less thoughts/` | 引用卡片（观点 + 时间） |
| 5 | 项目 | `$ ls projects/` | 卡片网格（名称、简介、star、license） |
| 6 | **全球足迹** | `$ ./modules/footprint --render` | 可交互 3D 地球下钻（详见第 5 节） |
| 7 | 笔记 / Blog | `$ ls -lt notes/` | 文章列表（日期、标题、标签） |
| 8 | 页脚 | `$ echo ...` | 版权、托管说明 |

---

## 5. 核心模块详述：全球足迹（Footprint Globe）

### 5.1 功能描述
一个可交互的 3D 地球仪，展示「去过的地方」，支持逐级下钻查看真实地理版图。

### 5.2 交互层级（三层，整层替换）
```
[ 地球仪 ]  --点击国家-->  [ 国家版图 ]  --点击省/州-->  [ 城市地图 ]
    ^                          |                            |
    |------- 面包屑/返回 --------+--------- 面包屑/返回 --------+
```
- **首层 · 地球仪**：可拖拽旋转、自动自转（带惯性），去过的国家用琥珀色脉冲光点标记。
- **第二层 · 国家版图**：点击光点或标签进入；显示该国真实行政边界（省/州/都道府县），去过的区域绿色高亮，可下钻的区域发亮可点。
- **第三层 · 城市地图**：点击可下钻区域进入；显示该省/州内真实市/区/县边界，去过的城市绿色高亮，悬停显示名称。
- **切层即替换**：进入下一层时当前视图整体消失（非叠加），离开地球时暂停 canvas 动画、返回时恢复；切层后自动滚动定位到模块顶部。
- **返回**：顶部面包屑（`~/globe / CN / 湖南省`）任意一级可点返回；每层左上角 `$ cd ..` 返回上一级。

### 5.3 数据范围（当前）
| 层级 | 支持范围 |
|---|---|
| 国家 | 中国、日本、马来西亚、新加坡（4 国） |
| 下钻深度 | **仅到城市级**（国家 → 城市，2 级下钻） |
| 中国可下钻省份 | 北京、湖南、广东、浙江、四川、江苏 |
| 日本可下钻 | 东京都、大阪府 |
| 马来西亚可下钻 | 雪兰莪、沙巴 |
| 新加坡 | 全域（55 规划区） |

> 当前点亮的城市为**随机示例**，正式版将替换为本人真实去过的地点。

### 5.4 地球渲染规格
- **实现**：Canvas 2D，经纬度 → 球面 3D 投影 + Y/X 轴旋转矩阵。
- **陆地**：Natural Earth 110m 世界陆地轮廓（简化后内联）。
- **国界**：Natural Earth 110m 国家边界（简化后内联），叠加在大洲之上以细淡线区分各国。
- **体积感**：海洋径向渐变（近亮远暗）模拟球面受光；球体边缘荧光光圈。
- **背面剔除**：转到球体背面的线段自动断开，产生真实遮挡感（大陆随旋转「转出/转入」）。
- **标记**：去过国家用琥珀色呼吸脉冲点 + 国家代码标签。

### 5.5 版图数据规格
- **来源**：中国省/市 GeoJSON（DataV）；日本/马来西亚/新加坡行政边界（geoBoundaries ADM1/ADM2）。
- **处理**：Douglas-Peucker 简化 → 等距圆柱投影（含纬度余弦修正）→ 归一化为 SVG viewBox → 内联进页面。
- **父子归属**：无父级字段的 ADM2（市/区）通过质心点-在-多边形（point-in-polygon）判定归入对应省/州。
- **运行时零网络**：所有地理数据内联，离线可用。

---

## 6. 内容模型（后续填充真实数据）

| 模块 | 字段 |
|---|---|
| whoami | 姓名、职位、简介、技术栈标签 |
| 经历 | 时间段、公司、职位、职责描述 |
| 思考 | 观点文本、主题、日期 |
| 项目 | 名称、简介、语言、star 数、license、链接 |
| 足迹 | 国家 → 省/州 → 城市 的真实去过列表 |
| 笔记 | 日期、标题、标签、正文（Markdown） |

> 原型中以 `{{ }}` 占位示例标注，待替换为真实内容。

---

## 7. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 单文件 ~270KB（含全部内联地理数据），首屏无需额外请求（仅 Google Fonts CDN） |
| 兼容性 | 现代浏览器（Chrome/Safari/Firefox 最新版）；桌面 + 移动端 |
| 可维护性 | 原型为单 HTML；正式版拆分为可用 Markdown 写博客的静态站点 |
| 可迁移性 | 静态产物，可在任意静态托管间迁移，不锁定平台 |
| 隐私 | 无追踪、无第三方分析（除自选） |

---

## 8. 技术方案

### 8.1 原型阶段（当前）
- 单文件 HTML + 内联 CSS/JS + 内联地理数据，零构建、零依赖（仅 Google Fonts）。

### 8.2 正式版建议
- **框架**：Astro（内容站首选，支持 Markdown/MDX，输出纯静态）。
- **博客**：Markdown 文件即文章，frontmatter 管理标题/日期/标签。
- **托管（免费起步）**：GitHub 仓库 + Cloudflare Pages（或 GitHub Pages）自动部署。
- **迁移路径**：静态产物（`dist/`）可直接搬到自有服务器（Nginx/Caddy）+ 自有域名，CI 不变。
- **足迹模块**：作为独立组件嵌入，地理数据与「去过列表」分离，便于更新。

---

## 9. 里程碑

| 阶段 | 交付物 | 状态 |
|---|---|---|
| M1 | 足迹球交互原型（4 国、城市下钻、真实版图） | ✅ 已完成 |
| M2 | 复古终端博客首页 + 足迹球整合 | ✅ 已完成（原型） |
| M3 | 替换真实内容（经历/项目/文章/足迹数据） | ⏳ 待办 |
| M4 | 迁移到 Astro 项目 + Markdown 博客工作流 | ⏳ 待办 |
| M5 | GitHub + Cloudflare Pages 部署上线 | ⏳ 待办 |
| M6 | 迁移至自有域名与服务器 | ⏳ 规划中 |

---

## 10. 待确认事项（Open Questions）

1. 博客模块是否需要**分类/归档/RSS**？
2. 足迹是否要展示**统计数据**（去过 N 国 / N 城）与**旅行时间线**？
3. 是否需要**深色/浅色以外的主题切换**（当前仅 CRT 绿）？
4. 项目模块是否要**自动拉取 GitHub star 数**（引入少量动态）？
5. 域名与服务器是否已就绪，M6 的时间点？

---

*本文档基于已确认的原型整理，占位内容与随机示例待替换为真实数据。*
